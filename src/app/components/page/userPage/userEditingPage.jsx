import React from "react";
import PropTypes from "prop-types";

const UserEditingPage = ({ id }) => {
    return <h1>User editing</h1>;
};

UserEditingPage.propTypes = {
    id: PropTypes.string.isRequired
};

export default UserEditingPage;
